// Min path cover (DAG) — reduction sketch (placeholder)
// When: cover all vertices with fewest vertex-disjoint paths; build split graph + max matching.
// Time: O(VE) if using Kuhn/Dinic on the split network (same order as matching + flow)
// Space: O(V+E) for the flow graph
#include <cassert>

int main() {
    assert(true);
    return 0;
}
